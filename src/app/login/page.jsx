export const metadata = {
     title: "Doc Appoint | Login",
     description: "A platform to book doctor appointment",
};

import LoginClient from "./LoginClient";

export default function Page() {
     return <LoginClient />;
}