export const metadata = {
     title: "Doc Appoint | Register",
     description: "A platform to book doctor appointment",
};

import RegisterClient from "./RegisterClient";

export default function Page() {
     return <RegisterClient />;
}